# Edge Gateway Worker

A lightweight edge-runtime utility for routing generic API requests through Vercel's Edge Network. 

## Deployment Setup

Requires a single Environment Variable configured in the Vercel Dashboard:
- `UPSTREAM_HOST`: The target URL for the gateway (e.g., `https://api.example.com`).

## License
MIT
